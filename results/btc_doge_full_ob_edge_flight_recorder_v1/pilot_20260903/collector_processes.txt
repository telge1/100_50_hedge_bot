.venv/bin/python -m orderbook_analyse.orderbook_v2_live --mode raw-archive-only --symbols BTCUSDT,DOGEUSDT --confirm-raw-archive-symbols --health-file logs/orderbook_v3_raw_archive_btc_doge.health.ndjson --log-level INFO
/bin/bash -O extglob -c snap=$(command cat <&3) && builtin shopt -s extglob && builtin eval -- "$snap" && { builtin set +u 2>/dev/null || true; builtin eval "${__CURSOR_SANDBOX_ENV_RESTORE:-}" 2>/dev/null; builtin export PWD="$(builtin pwd)"; builtin shopt -s expand_aliases 2>/dev/null; builtin eval "$1" < /dev/null; }; COMMAND_EXIT_CODE=$?; dump_bash_state >&4; builtin exit $COMMAND_EXIT_CODE -- set -euo pipefail ROOT=/home/telgenbuescher/projects/orderbook_analyse PILOT=/home/telgenbuescher/projects/spread_recovery_hedge_short_dev/results/btc_doge_full_ob_edge_flight_recorder_v1/pilot_20260903 NEW_PID=$(tr -d ' \n' < "$ROOT/logs/orderbook_v3_raw_archive_only.pid") FR_ROOT="$ROOT/data/orderbook_raw_shadow/full_ob_edge_flight_recorder" mkdir -p "$PILOT/health_samples" "$PILOT/resource_samples"  # Fail-closed monitor for 15 minutes (900s), sample every 30s END=$((SECONDS+900)) N=0 FAIL=0 while (( SECONDS < END )); do   N=$((N+1))   TS=$(date -u +%Y%m%dT%H%M%SZ)   if ! kill -0 "$NEW_PID" 2>/dev/null; then     echo "FAIL_CLOSED process_dead pid=$NEW_PID" | tee "$PILOT/FAIL_CLOSED.txt"     FAIL=1; break   fi   # only one orderbook_v2_live raw-archive process   COUNT=$(ps -eo args | grep -c '[o]rderbook_v2_live --mode raw-archive-only' || true)   if (( COUNT != 1 )); then     echo "FAIL_CLOSED duplicate_or_missing_collector count=$COUNT" | tee "$PILOT/FAIL_CLOSED.txt"     FAIL=1; break   fi   # resources   RSS=$(awk '/VmRSS:/ {print $2}' /proc/$NEW_PID/status)   CPU=$(ps -p $NEW_PID -o %cpu= | tr -d ' ')   echo "$TS rss_kb=$RSS cpu=$CPU" >> "$PILOT/resource_samples/rss_cpu.txt"   # health extract   tail -n 1 "$ROOT/logs/orderbook_v3_raw_archive_btc_doge.health.ndjson" > "$PILOT/health_samples/$TS.json"   python3 - <<PY import json from pathlib import Path o=json.loads(Path("$PILOT/health_samples/$TS.json").read_text()) fail=False notes=[] if o.get('fail_closed'):   fail=True; notes.append('collector_fail_closed') if not o.get('connected'):   fail=True; notes.append('disconnected') fr=o.get('full_ob_flight_recorder_enabled') if fr is not True:   fail=True; notes.append('fr_disabled') root=o.get('capture_root') or '' if 'full_ob_edge_flight_recorder' not in root:   fail=True; notes.append(f'bad_root:{root}') for rt in o.get('full_book_runtimes') or []:   if not rt.get('book_ready'):     fail=True; notes.append(f"not_ready:{rt.get('symbol')}")   if int(rt.get('gap_count') or 0) > 5:     fail=True; notes.append(f"gaps:{rt.get('symbol')}={rt.get('gap_count')}")   if int(rt.get('reconnect_count') or 0) > 3:     fail=True; notes.append(f"reconnects:{rt.get('symbol')}={rt.get('reconnect_count')}") # RSS hard stop ~2.5GB rss=int("$RSS") if rss > 2_500_000:   fail=True; notes.append(f'rss_kb={rss}') print(json.dumps({'ts':'$TS','fail':fail,'notes':notes,'rss_kb':rss,'cpu':'$CPU','active_topics':o.get('full_book_active_topics'),'runtimes':o.get('runtimes'),'books':[{'s':r.get('symbol'),'ready':r.get('book_ready'),'u':r.get('update_id'),'gaps':r.get('gap_count'),'bids':r.get('raw_bids'),'asks':r.get('raw_asks'),'state':r.get('subscription_state')} for r in o.get('full_book_runtimes') or []]}, default=str)) if fail:   Path("$PILOT/FAIL_CLOSED.txt").write_text('\\n'.join(notes)+'\\n')   raise SystemExit(2) PY   RC=$?   if (( RC != 0 )); then     echo "FAIL_CLOSED health_gate" | tee -a "$PILOT/FAIL_CLOSED.txt"     FAIL=1; break   fi   # artifact growth only under FR root   find "$FR_ROOT" -type f 2>/dev/null | wc -l > "$PILOT/resource_samples/fr_filecount_$TS.txt"   # ensure no writes outside intended tree from open *.tmp in wrong places   sleep 30 done  echo "monitor_done fail=$FAIL samples=$N elapsed_approx=$((N*30))s" | tee "$PILOT/monitor_summary.txt" # final health tail -n 1 "$ROOT/logs/orderbook_v3_raw_archive_btc_doge.health.ndjson" | python3 -m json.tool > "$PILOT/health_t15.json" || true # list artifacts find "$FR_ROOT" -type f 2>/dev/null | sort | tee "$PILOT/artifacts_list.txt" | head -80 du -sh "$FR_ROOT" 2>/dev/null | tee "$PILOT/artifacts_du.txt" ps -p "$NEW_PID" -o pid,%cpu,%mem,rss,etime,cmd # foreign processes still untouched? kill -0 147111 && echo oi_still=147111 || echo oi_gone # count collectors ps -eo args | grep '[o]rderbook_v2_live' | tee "$PILOT/collector_processes.txt" exit $FAIL 
